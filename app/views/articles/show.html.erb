
<div class="containter" style="margin: 40px;">

<div class="card m-3 mt-5">
  <div class="card-body">
    <h2><%= @article.title %></h2>
    <p><%= @article.content %></p>
    <%= link_to "⚙️", edit_article_path, class: "btn btn-light"%>
    <%= link_to "🗑️", article_path(@article), data:{turbo_method: :delete, turbo_confirm: "Delete #{@article.title}?" }, class: "btn btn-light"%>
  </div>
</div>

<%= link_to "Home", articles_path, class: "btn btn-large btn-secondary ms-5" %>

</div>
